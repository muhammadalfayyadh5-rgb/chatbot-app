version: '3.8'

services:
  server:
    build: ./server
    container_name: chatbot-server
    ports:
      - "8081:8080"

  client:
    build: ./client
    container_name: chatbot-client
    depends_on:
      - server